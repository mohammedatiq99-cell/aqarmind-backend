# Aqarmind Architecture

## Request flow

1. A user interacts with the React chat experience.
2. The frontend sends a request to FastAPI.
3. FastAPI applies input controls and forwards conversational work to the model layer.
4. The model can request approved tools rather than inventing structured facts.
5. Tool execution is performed by backend Python functions.
6. Structured property queries are executed against Azure SQL.
7. Knowledge questions are grounded through Azure AI Search / RAG.
8. Deterministic calculations are handled outside the LLM.
9. The model receives tool results and produces a user-facing answer.

## Design principles

- **Structured data stays structured:** property filters should query SQL rather than RAG.
- **Knowledge retrieval stays grounded:** brochures, FAQs and guides are retrieved from a search index.
- **Math stays deterministic:** mortgage calculations are performed by code rather than generated by an LLM.
- **LLMs orchestrate, backend services execute:** tool calls are validated and run server-side.
- **Secrets stay outside source control:** configuration is injected through environment variables / secure secret stores.
- **Public code is not production configuration:** resource names, credentials, customer data and private operational details are excluded.

## Production-oriented extensions

The full product direction can extend this foundation with:

- multi-tenant configuration
- admin / lead management portal
- per-tenant usage quotas
- managed identity and Key Vault
- structured observability and tracing
- content / response guardrails
- audit logs for tool execution
- ingestion pipelines for customer documents
- deployment slots and controlled releases
